<!doctype html>
<html class="no-js" lang="">

<head>
  <link rel="stylesheet" href="/css/store-{{gitSha}}.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Special+Elite&display=swap" rel="stylesheet">
  {% include 'content/head.jinja2' %}
</head>

<body>

  {% include 'content/header.jinja2' %}
  {% set page = 'store' %}
  {% include 'content/menu.jinja2' %}
    <div class="body">
        <div class="printItem">
            <div class="close-container" id="close-print">
                <div class="leftright"></div>
                <div class="rightleft"></div>
            </div>
            <h4 id="title"></h4>
            <div class="center">
                <div>
                    <a id="viewlink">
                        <div class="imageContainer">
                            <div id="image">
                            </div>
                        </div>
                    </a>
                </div>
                <div id="addtocart">
                    Add to cart
                </div>
                <select id="size">
                </select>
                <select id="signature">
                     <option value=0>Signature front</option>
                     <option value=1>Signature rear</option>
                     <option value=2>No signature!</option>
                </select>
            </div>
            <span id="price"></span>
            <span class="typewriter" id="info" >
                This work is printed on Hahnemühle's Fine-art Baryta paper, an exclusive paper that I've come to love for it's slightly textured and subtle glossy surface, and will be delivered rolled up together with a certificate describing the work.
            </span>
            <p id="description">
            </p>
        </div>
        <div class="printerror">
            <h4> Print not found :/ </h4>
            <p> The link might be wrong or the item has been removed from sale </p>
        </div>
    </div>
    {% include 'content/footer.jinja2' %}
    <script>
        document.write('<script src="/js/print.js"><\/script>');
    </script>
    {% include 'content/cart.jinja2' %}
</body>
</html>
