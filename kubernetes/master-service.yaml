apiVersion: v1
kind: Service
metadata:
  name: ingress
spec:
  type: NodePort
  ports:
   - name: http
     protocol: TCP
     port: 80
     nodePort: 30008
     targetPort: 80
  selector:
    app: web-gallery
