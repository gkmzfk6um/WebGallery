apiVersion: v1
kind: Service
metadata:
  name: webgallery-service
spec:
  type: NodePort
  ports:
   - name: http
     protocol: TCP
     port: 80
     targetPort: 80
     nodePort: 30008
  selector:
    app: web-gallery-nginx